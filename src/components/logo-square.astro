---
type Props = { size?: "sm" | undefined };
import clsx from "clsx";
import LogoIcon from "./icons/logo.astro";
---

<div
  class={clsx(
    "flex flex-none items-center justify-center",
    {
      "h-[60px] w-[60px] rounded-xl": !Astro.props.size,
      "h-[40px] w-[40px] rounded-lg": Astro.props.size === "sm",
    }
  )}
>
  <LogoIcon
    class={clsx({
      "h-[60px] w-[60px]": !Astro.props.size,
      "h-[40px] w-[40px]": Astro.props.size === "sm",
    })}
  />
</div>
